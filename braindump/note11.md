everything is a server either physical logical or objects. 

ie it is a type so all major routes and endpoints should have a controller server instance. 

```
    app {

        @Server Remote,
        @Server 

    }
```